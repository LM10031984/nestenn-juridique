---
phase: fusion-et-ameliorations
task: benchmark-rerun
status: in_progress
last_updated: 2026-03-19T06:14:01.680Z
---

<current_state>
Plan 2026-03-18-fusion-et-ameliorations.md entièrement exécuté (Tasks 1-9).
Benchmark lancé mais stoppé à 44/50 questions — le juge LLM (GPT-4o-mini via OpenRouter) retournait 401 car la clé OPENROUTER_API_KEY n'était pas chargée automatiquement dans le contexte tsx standalone.
</current_state>

<completed_work>

- Task 1 ✅ : lib/judilibre.ts — client PISTE/Judilibre avec cache token OAuth2
- Task 2 ✅ : lib/legifrance.ts — mode dégradé gracieux (null au lieu de throw)
- Task 3 ✅ : app/api/chat/route.ts — filtre hors-périmètre LLM (MODELS.FILTER)
- Task 4 ✅ : Pipeline parallèle DILA + Judilibre dans chat route
- Task 5 ✅ : lib/system-prompt.ts — 3 few-shot examples (copropriété, bail, Loi Hoguet)
- Task 6 ✅ : Feedback Supabase (migration 002_feedback.sql + /api/feedback + handleFeedback async)
- Task 7 ✅ : Dashboard admin (/api/admin/feedback-report + /admin/dashboard + layout guard)
- Task 8 ✅ : scripts/benchmark.ts + scripts/test-questions.json (50 questions, 9 thèmes)
- Task 9 ✅ : .env.example mis à jour, audit sécurité propre, commentaires PISTE
- PISTE credentials remplis et testés ✅ : OAuth2 token OK, Judilibre OK
- Repo GitHub créé et pushé ✅ : https://github.com/LM10031984/nestenn-juridique (privé)
</completed_work>

<remaining_work>

- Relancer le benchmark avec la clé OpenRouter chargée :
  ```bash
  OPENROUTER_API_KEY=$(grep OPENROUTER_API_KEY .env.local | cut -d= -f2) npx tsx scripts/benchmark.ts
  ```
- Analyser les questions faibles identifiées mécaniquement :
  Q23 (charges récupérables), Q24 (DPE F/G), Q29 (Airbnb copro),
  Q37 (conditions suspensives), Q41 (mandat exclusif), Q44 (Denormandie)
- Améliorer system prompt ou few-shot examples selon résultats
- Phase suivante (hors scope plan actuel) : RAG avec pgvector + text-embedding-3-large
</remaining_work>

<decisions_made>

- Token OAuth2 PISTE renouvelé automatiquement (cache in-memory, marge 60s) — pas de problème en production
- Le juge LLM du benchmark utilise GPT-4o-mini via OpenRouter — retourne boolean|null (null = erreur, pas un échec)
- Benchmark stoppé à 44q car résultats mécaniques suffisants pour identifier les lacunes
- Résultats mécaniques sur 44q : refs=✓ 64% (28/44), kw=✓ 75% (33/44)
</decisions_made>

<blockers>

- Benchmark juge 401 : tsx ne charge pas .env.local automatiquement — solution : passer OPENROUTER_API_KEY en variable d'environnement inline (voir commande ci-dessus)
</blockers>

<context>
Le plan fusion-et-ameliorations est 100% implémenté et pushé. La prochaine priorité est de valider la qualité des réponses via le benchmark complet avec juge LLM, puis d'améliorer les points faibles avant de passer à la phase RAG (pgvector). L'application tourne sur Next.js 14 + Supabase + OpenRouter. Les credentials PISTE (Légifrance + Judilibre) sont opérationnels en production.
</context>

<next_action>
Relancer le benchmark avec la clé OpenRouter :
```bash
cd C:/Users/laure/Documents/Projet-claude/MJA/nestenn-juridique
OPENROUTER_API_KEY=$(grep OPENROUTER_API_KEY .env.local | cut -d= -f2) npx tsx scripts/benchmark.ts
```
Puis analyser le rapport final (taux global + par thème + questions échouées).
</next_action>
